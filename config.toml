
baseURL = "/"
languageCode = "en-gb"
title = "stuart lee"
theme = "paperesque"
copyright = "© 2017 - 2020 Stuart Lee"
enableEmoji = true

# I can't remember if this is required for syntax highlighting or not.
# I decided to use highlight js instead, so this isn't needed
# pygmentsUseClasses = true


# for blogdown
ignoreFiles = ["\\.Rmd$", "\\.Rmarkdown$", "_files$", "_cache$"]

[permalinks]
    post = "/:year/:month/:day/:slug/"

[markup]
  # This is the default from Hugo 0.60.0 onwards, but we're setting config
  # for it below, so make it explicit
  defaultMarkdownHandler = "goldmark"

# Required to render HTML tags in the hugoExampleSite examples.
[markup.goldmark.renderer]
  unsafe = true

# Uncomment these lines for droplist / resource removal support! See the
# README for more details

# [outputs]
# page = ["HTML", "droplist"]

[params]
# Turn this on if you want to disable margin notes everywhere!
disableMarginNotes = true

# Customise your home page menu here:

[[params.menu]]
  name = "about"
  url = "about/"

[[params.menu]]
  name = "writing"
  url = "post/"

[[params.menu]]
  name = "publications"
  url = "publications/"

[[params.menu]]
  name = "software"
  url = "https://github.com/sa-lee/"

[[params.menu]]
  name = "tags"
  url = "tags/"

# And your top-right menu links here.
[[params.topmenu]]
  name = "about"
  url = "about/"
  
[[params.topmenu]]
  name = "contact"
  url = "contact/"

